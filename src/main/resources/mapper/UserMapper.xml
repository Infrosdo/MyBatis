<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="user">

    <!-- 普通查询 -->
    <select id="selectOne" parameterType="int" resultType="user">
        SELECT *
        FROM user
        WHERE id = #{id}
    </select>

    <!-- resultMap用法 -->
    <!-- 1、constructor -->
    <resultMap id="constructor" type="user">
        <constructor>
            <idArg column="id" javaType="_int"/>
            <arg column="name" javaType="String"/>
            <arg column="age" javaType="_int"/>
        </constructor>
    </resultMap>

    <select id="consSelect" parameterType="int" resultMap="constructor">
        SELECT *
        FROM user
        WHERE id = #{id}
    </select>

    <!-- 普通添加 -->
    <insert id="insert" parameterType="User">
        insert into user (name, age)
        values (#{name}, #{age})
    </insert>

    <!-- 主键返回2种方法 -->
    <insert id="generateKey" useGeneratedKeys="true" keyProperty="id">
        <!--<selectKey
                keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            select LAST_INSERT_ID()
        </selectKey>-->

        insert into user (name, age)
        values (#{name}, #{age})
    </insert>

    <update id="updateUser">
        update user
        set name = #{name},
            age  = #{age}
        where id = #{id}
    </update>
</mapper>